version: "3.9"

services:
  approval-service:
    build: .
    container_name: approval-service
    environment:
      DATABASE_URL: sqlite:///./data/app.db
      LOG_LEVEL: INFO
    ports:
      - "8000:8000"
    volumes:
      - approval_data:/app/data

volumes:
  approval_data:
